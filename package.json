{
  "name": "js-bard",
  "version": "0.2.2",
  "description": "",
  "main": "/src/Bard.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "budo index.js:bundle.js --live -- -t ",
    "build": "browserify  -t glslify -t [babelify --presets [ es2015 ]] index.js > bundle.js  "
  },
  "author": "solaldr",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {},
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ],
          "plugins": [
            "glslify"
          ]
        }
      ]
    ]
  }
}
